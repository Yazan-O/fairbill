# Submission assets

- `../README.md`: the repo README, at the root because GitHub reads it there. Title, badges, hero capture and QR, the flow GIF, the problem with sourced numbers, the six gallery bills, how it works, both diagrams, the bench table, run-locally commands, data sources, the fictional-parts statement, and the license line.
- `README.md`: a two-line pointer to the root README. One source of truth.
- `VIDEO_SCRIPT.md`: the demo video shot list, GENERATED and REAL tags, timings, voice-over, and a table mapping every spoken number to its source.
- `DEVPOST.md`: the Devpost form fields, ready to paste, one block per field.
- `BLOG_builder_aws.md`: the builder.aws post, "Agents for Humans: teaching an agent to read a hospital's price file", with code from the shipped source.
- `architecture.html`: the architecture diagram source, inline SVG.
- `architecture.svg`: the exported architecture diagram, embedded by the root README.
- `request_path.svg`: the request-path diagram (phone, Lambda Function URL, AgentCore Runtime, Amazon Bedrock, event stream back), embedded by the root README.
- `qr_live.png` (540 px) and `qr_live.svg`: the QR code for the live URL.
- `media/`: real screen captures of the live link, owned by the capture session. The root README embeds `media/hero_phone.png`, `media/audit_row_phone.png` and `media/flow.gif`.
- `../LICENSE`: MIT, copyright 2026 Mohamad Yazan Sadoun.

Placeholders still open across these files: `[REPO_URL]`, `[VIDEO_URL]`, `[BLOG_URL]`.
